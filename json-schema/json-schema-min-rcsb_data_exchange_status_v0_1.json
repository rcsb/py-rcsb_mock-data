{
   "type": "object",
   "properties": {
      "database_name": {
         "type": "string",
         "description": "Name of the database container for the current data exchange status record."
      },
      "object_name": {
         "type": "string",
         "description": "Name of the database object for the current data exchange status record."
      },
      "update_begin_timestamp": {
         "type": "string",
         "format": "date-time",
         "description": "Starting timestamp for the current data exchange status record."
      },
      "update_end_timestamp": {
         "type": "string",
         "format": "date-time",
         "description": "Ending timestamp for the current data exchange status record."
      },
      "update_id": {
         "type": "string",
         "description": "Identifier for the current data exchange status record."
      },
      "update_status_flag": {
         "type": "string",
         "enum": [
            "Y",
            "N"
         ],
         "description": "Success status for the current data exchange status record."
      }
   },
   "required": [
      "update_id"
   ],
   "$id": "https://github.com/rcsb/py-rcsb.db/tree/master/rcsb.db/data/json-schema/",
   "$schema": "http://json-schema.org/draft-04/schema#",
   "title": "Schema for content type data_exchange collection rcsb_data_exchange_status_v0_1"
}